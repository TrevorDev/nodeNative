#hdr
#include <iostream>
#include "src/vulkanHelper.h"
#end

using namespace std;  

int main()  
{  
	VulkanHelper* v = new VulkanHelper();
	v->initDevice();
	uint32_t bufferSize = sizeof(int32_t) * 10;
	v->allocGPUMemory(bufferSize);
    cout << v->num << endl;
}  
